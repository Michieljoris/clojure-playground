(ns testreact.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Project testreact says: Hello, World!" )
  (println "We're in the following namespace: testreact.core")
  )
